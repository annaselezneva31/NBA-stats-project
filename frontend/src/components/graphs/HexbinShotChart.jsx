import React, { useEffect, useRef } from "react";
import * as d3 from "d3";
import { hexbin } from "d3-hexbin";

const WIDTH = 500;
const HEIGHT = 420;
const BIN_SIZE = 6;

const HexbinShotChart = ({ shotData }) => {
  console.log(shotData);
  return <div></div>;
};

export default HexbinShotChart;
